//primitive Data types 
// 7 types : String, Number, Bollearn, null, undefined, Symbol BigInt

// Refrence (Not primitive)
// Arrray, objects , functions

const score = 100
const scoreValue = 10.2

const isLoggedIn = false

const outsideTemp = null

let userEmail;

const id = Symbol('123')
const anotherId = Symbol('123')
console.log(id === anotherId);
